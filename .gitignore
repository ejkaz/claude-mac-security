# Scan artifacts / reports are machine-specific — keep them out of the public repo.
*.report.md
audits/
reports/
*.pcap
.DS_Store

# Never commit secrets / keys
.vt.toml
*.key
.env

# Local-only agent context pointer (references private vault hub — do not publish)
CLAUDE.md
