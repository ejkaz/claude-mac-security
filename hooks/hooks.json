{
  "_comment": "OPTIONAL hardening hooks — DISABLED by default. This plugin's spine is read-only/on-demand; it does not silently police your shell. To enable a guard, move an entry from _disabled into hooks and reload. Example below blocks pipe-to-shell installs (curl|sh) and rm -rf at the PreToolUse stage.",
  "hooks": {},
  "_disabled": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/guard-bash.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
