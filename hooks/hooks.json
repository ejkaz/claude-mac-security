{
  "_comment": "Hardening guard — ENABLED as of v0.2 (MSS-D005). guard-bash.sh blocks pipe-to-shell installs (curl|sh) and rm -rf at PreToolUse; everything else passes through. Move the entry back to _disabled to turn it off.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/bin/guard-bash.sh",
            "timeout": 5
          }
        ]
      }
    ]
  },
  "_disabled": {}
}
